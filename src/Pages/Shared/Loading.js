import React from "react";
import spinner from "../../asset/images/Spinner/spinner.gif";

const Loading = () => {
    return <img className="w-full" src={spinner} alt="" />;
};

export default Loading;
